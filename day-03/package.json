{
  "name": "ucp-merchant-day-03",
  "version": "1.0.0",
  "description": "Day 3: POST /checkout-sessions — create a checkout",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node server.js & sleep 1 && node tests/checkout.test.js"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
